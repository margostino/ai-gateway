{
  "watch": ["src", "package.json", "yarn.lock", ".env"],
  "ext": "ts, js",
  "execMap": {
    "ts": "node --require ts-node/register -r tsconfig-paths/register"
  },
  "env": {
    "TS_NODE_PROJECT": "tsconfig.dev.json",
    "NODE_OPTIONS": "--inspect"
  }
}
